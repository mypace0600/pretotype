
server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/hair?serverTimezone=Asia/Seoul
    username: hair
    password: 1234

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create #???? create? ???? ???? update? ???? ? , ??? mysql? ?????? ?
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl #entity? ?? ? ? ???? ?? ? ?? ??? db? ?????
      use-new-id-generator-mappings: false # jpa? ?? ??? ??? ???? ???
    show-sql: true
    properties:
      hibernate.format_sql: true # ??? ?? ???? ?

    jackson:
      serialization:
        fail-on-empty-beans: false
